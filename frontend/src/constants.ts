/**
 * Stable UUID of the single workspace root block.
 * Matches the seed inserted in migration 0002 and the backend constant
 * app.blocks.models.WORKSPACE_ROOT_ID.
 */
export const WORKSPACE_ROOT_ID: string = '00000000-0000-0000-0000-000000000001'
